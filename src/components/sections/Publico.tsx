import { Box } from '@chakra-ui/react'

export default function Publico() {
  return (
    <Box as="section" id="publico" py={{ base: 10, md: 14 }}>
      {/* Conteúdo da seção Público */}
    </Box>
  )
}