import { Box } from '@chakra-ui/react'

export default function Pacotes() {
  return (
    <Box as="section" id="pacotes" py={{ base: 10, md: 14 }}>
      {/* Conteúdo da seção Pacotes */}
    </Box>
  )
}