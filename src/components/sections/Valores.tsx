import { Box } from '@chakra-ui/react'

export default function Valores() {
  return (
    <Box as="section" id="valores" py={{ base: 10, md: 14 }}>
      {/* Conteúdo da seção Valores */}
    </Box>
  )
}